<%= form_with(model: user_region, local: true) do |form| %>
  <% if user_region.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_region.errors.count, "error") %> prohibited this user_region from being saved:</h2>

      <ul>
      <% user_region.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id, id: :user_region_user_id %>
  </div>

  <div class="field">
    <%= form.label :region_id %>
    <%= form.number_field :region_id, id: :user_region_region_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
